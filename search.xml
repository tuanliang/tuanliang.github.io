<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2024/04/20/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>spring</title>
    <url>/2024/04/20/spring/</url>
    <content><![CDATA[<h2 id="三、原理解析"><a href="#三、原理解析" class="headerlink" title="三、原理解析"></a>三、原理解析</h2><h3 id="3-1-结构分析"><a href="#3-1-结构分析" class="headerlink" title="3.1 结构分析"></a>3.1 结构分析</h3><p>Spring Security 所解决的问题就是<strong>安全访问控制</strong>，而安全访问控制功能其实就是对所有进入系统的请求进行拦截，校验每个请求是否能够访问它所期望的资源。根据前边知识的学习，可以通过 Filter 或 AOP 等技术来实现，Spring Security 对 Web 资源的保护是靠 Filter 实现的，所以从这个 Filter 来入手，逐步深入 Spring Security 原理。</p>
<p>当初始化 Spring Security 时，会创建一个名为 SpringSecurityFilterChain 的 Servlet 过滤器，类型为 org.springframework.security.web.FilterChainProxy，它实现了 javax.servlet.Filter，因此外部的请求会经过此类，下图是 Spring Security 过虑器链结构图：</p>
<p>![在这里插入图片描述](<a href="https://img-blog.csdnimg.cn/b6001096cbeb48b0967942d962a93770.png">https://img-blog.csdnimg.cn/b6001096cbeb48b0967942d962a93770.png</a> &#x3D;350x350)</p>
<p>Spring Security 功能的实现主要是由一系列过滤器链相互配合完成。</p>
<p><img src="https://img-blog.csdnimg.cn/210ed768cf634344a92849f806c1dad2.png" alt="在这里插入图片描述"></p>
<p>下面介绍过滤器链中主要的几个过滤器及其作用：</p>
<p><strong>SecurityContextPersistenceFilter</strong> 这个 Filter 是整个拦截过程的入口和出口（也就是第一个和最后一个拦截器），会在请求开始时从配置好 SecurityContextRepository 中获取 SecurityContext，然后把它设置给 SecurityContextHolder。在请求完成后将 SecurityContextHolder 持有的 SecurityContext 再保存到配置好 的 SecurityContextRepository，同时清除 securityContextHolder 所持有的 SecurityContext；</p>
<p><strong>UsernamePasswordAuthenticationFilter</strong> 用于处理来自表单提交的认证。该表单必须提供对应的用户名和密 码，其内部还有登录成功或失败后进行处理的 AuthenticationSuccessHandler 和 AuthenticationFailureHandler，这些都可以根据需求做相关改变；</p>
<p><strong>FilterSecurityInterceptor</strong> 是用于保护 web 资源的，使用 AccessDecisionManager 对当前用户进行授权访问；</p>
<p><strong>ExceptionTranslationFilter</strong> 能够捕获来自 FilterChain 所有的异常，并进行处理。但是它只会处理两类异常： AuthenticationException 和 AccessDeniedException，其它的异常它会继续抛出。</p>
<h3 id="3-1-登录认证流程分析"><a href="#3-1-登录认证流程分析" class="headerlink" title="3.1 登录认证流程分析"></a>3.1 登录认证流程分析</h3><p><img src="https://img-blog.csdnimg.cn/1de80c86f1574af6bfaf4bd6971889fd.jpeg" alt="在这里插入图片描述"></p>
<p>让我们仔细分析认证过程：</p>
<ol>
<li><p>用户提交用户名、密码被 SecurityFilterChain 中的 UsernamePasswordAuthenticationFilter 过滤器获取到， 封装为请求 Authentication，通常情况下是 UsernamePasswordAuthenticationToken 这个实现类。</p>
</li>
<li><p>然后过滤器将 Authentication 提交至认证管理器（AuthenticationManager）进行认证</p>
</li>
<li><p>认证成功后， AuthenticationManager 身份管理器返回一个被填充满了信息的（包括上面提到的权限信息，身份信息，细节信息，但密码通常会被移除） Authentication 实例。</p>
</li>
<li><p>SecurityContextHolder 安全上下文容器将第 3 步填充了信息的 Authentication ，通过 SecurityContextHolder.getContext().setAuthentication(…)方法，设置到其中。 可以看出 AuthenticationManager 接口（认证管理器）是认证相关的核心接口，也是发起认证的出发点，它 的实现类为 ProviderManager。而 Spring Security 支持多种认证方式，因此 ProviderManager 维护着一个 List<AuthenticationProvider> 列表，存放多种认证方式，最终实际的认证工作是由 AuthenticationProvider 完成的。咱们知道 web 表单的对应的 AuthenticationProvider 实现类为 DaoAuthenticationProvider，它的内部又维护着一个 UserDetailsService 负责 UserDetails 的获取。最终 AuthenticationProvider 将 UserDetails 填充至 Authentication。 认证核心组件的大体关系如下：</p>
</li>
</ol>
<p>流程图简易图：</p>
<p><img src="https://img-blog.csdnimg.cn/6462d725b49441a2a2f5568330868701.png" alt="在这里插入图片描述"></p>
<h2 id="四、会话管理"><a href="#四、会话管理" class="headerlink" title="四、会话管理"></a>四、会话管理</h2><h3 id="4-1-获取用户身份"><a href="#4-1-获取用户身份" class="headerlink" title="4.1 获取用户身份"></a>4.1 获取用户身份</h3><p>用户认证通过后，为了避免用户的每次操作都进行认证可将用户的信息保存在会话中。spring security 提供会话管理，认证通过后将身份信息放入 SecurityContextHolder 上下文，SecurityContext 与当前线程进行绑定，方便获取 用户身份。</p>
<p><strong>编写方法：</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping(&quot;/getUsername&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">getUsername</span><span class="params">()</span>&#123;</span><br><span class="line">        <span class="type">Authentication</span> <span class="variable">authentication</span> <span class="operator">=</span> SecurityContextHolder.getContext().getAuthentication();</span><br><span class="line">        <span class="type">Object</span> <span class="variable">principal</span> <span class="operator">=</span> authentication.getPrincipal();</span><br><span class="line">        <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line">        <span class="keyword">if</span>(principal <span class="keyword">instanceof</span> UserDetails)&#123;</span><br><span class="line">            username = ((UserDetails) principal).getUsername();</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            username=  principal.toString();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> username;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-2-会话控制"><a href="#4-2-会话控制" class="headerlink" title="4.2 会话控制"></a>4.2 会话控制</h3><p>我们可以通过以下选项准确控制会话何时创建以及 Spring Security 如何与之交互：</p>
<table>
<thead>
<tr>
<th align="left">机制</th>
<th align="left">描述</th>
</tr>
</thead>
<tbody><tr>
<td align="left">always</td>
<td align="left">如果没有 session 存在就创建一个</td>
</tr>
<tr>
<td align="left">ifRequired</td>
<td align="left">如果需要就创建一个 Session（默认）登录时</td>
</tr>
<tr>
<td align="left">never</td>
<td align="left">SpringSecurity 将不会创建 Session，但是如果应 用中其他地方创建了 Session，那么 Spring Security 将会使用它。</td>
</tr>
<tr>
<td align="left">stateless</td>
<td align="left">SpringSecurity 将绝对不会创建 Session，也不使用 Session</td>
</tr>
</tbody></table>
<p>通过以下配置方式对该选项进行配置：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">.and()            .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS);</span><br></pre></td></tr></table></figure>

<p>默认情况下，Spring Security 会为每个登录成功的用户会新建一个 Session，就是<strong>ifRequired</strong> 。</p>
<p>若选用<strong>never</strong>，则指示 Spring Security 对登录成功的用户不创建 Session 了，但若你的应用程序在某地方新建了 session，那么 Spring Security 会用它的。</p>
<p>若使用<strong>stateless</strong>，则说明 Spring Security 对登录成功的用户不会创建 Session 了，你的应用程序也不会允许新建 session。并且它会暗示不使用 cookie，所以每个请求都需要重新进行身份验证。这种无状态架构适用于 REST API 及其无状态认证机制。</p>
<h2 id="五、-RBAC-中集成认证和授权"><a href="#五、-RBAC-中集成认证和授权" class="headerlink" title="五、 RBAC 中集成认证和授权"></a>五、 RBAC 中集成认证和授权</h2><h3 id="5-1-集成认证"><a href="#5-1-集成认证" class="headerlink" title="5.1 集成认证"></a>5.1 集成认证</h3><p>####5.1.1 导入依赖</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-security<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>启动项目，发现前端界面在访问的时候出现了跨域问题。</p>
<p>原因： 因为跨域会发送一个预请求看服务端是否支持跨域， 但是这个预请求也会被拦截，之前我们在在拦截器中判断是否是 handlerMethod 决定是否放行，但是现在我们用的是 SpringSecurity ，是让 SpringSecurity 给拦截了。</p>
<h4 id="5-1-2-进行配置规则"><a href="#5-1-2-进行配置规则" class="headerlink" title="5.1.2 进行配置规则"></a>5.1.2 进行配置规则</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">void</span> <span class="title function_">configure</span><span class="params">(HttpSecurity http)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//进制 crsf</span></span><br><span class="line">        http.csrf().disable();</span><br><span class="line">        <span class="comment">//配置拦截规则</span></span><br><span class="line">        http.authorizeRequests().</span><br><span class="line">                antMatchers(<span class="string">&quot;/api/code&quot;</span>,<span class="string">&quot;/api/login&quot;</span>,<span class="string">&quot;/api/logout&quot;</span>).</span><br><span class="line">                permitAll().</span><br><span class="line">                anyRequest().</span><br><span class="line">                authenticated();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>重启访问： 验证码已经可以出来了， 但是点击登录调用 login 还是之前我们自己写的 login 方法，我们要让 SpringSecurity 帮我们做认证，注释之前在 LoginServiceImpl 实现类中登录的代码。</p>
<h3 id="5-2-集成授权"><a href="#5-2-集成授权" class="headerlink" title="5.2 集成授权"></a>5.2 集成授权</h3><h4 id="5-2-1-查询授权信息"><a href="#5-2-1-查询授权信息" class="headerlink" title="5.2.1 查询授权信息"></a>5.2.1 查询授权信息</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> Collection&lt;? <span class="keyword">extends</span> <span class="title class_">GrantedAuthority</span>&gt; getAuthorities() &#123;</span><br><span class="line">    <span class="comment">// 先查询出来当前用户是否是超级管理员</span></span><br><span class="line">    <span class="type">PermissionMapper</span> <span class="variable">permissionMapper</span> <span class="operator">=</span> SpringUtils.getBean(PermissionMapper.class);</span><br><span class="line">    List&lt;GrantedAuthority&gt; list = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">    <span class="keyword">if</span>(employee.isAdmin())&#123;</span><br><span class="line">        <span class="comment">// 如果是分配所有权限</span></span><br><span class="line">        List&lt;Permission&gt; permissions = permissionMapper.selectAll();</span><br><span class="line">        <span class="comment">// 如果不是分配用户所拥有的权限</span></span><br><span class="line">        <span class="keyword">for</span> (Permission permission : permissions) &#123;</span><br><span class="line">            list.add(<span class="keyword">new</span> <span class="title class_">SimpleGrantedAuthority</span>(permission.getExpression()));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">//根据用户id 查询用户所拥有权限结合</span></span><br><span class="line">        List&lt;String&gt; expressions = permissionMapper.queryPermissionByEmpId(employee.getId());</span><br><span class="line">        <span class="keyword">for</span> (String expression : expressions) &#123;</span><br><span class="line">            list.add(<span class="keyword">new</span> <span class="title class_">SimpleGrantedAuthority</span>(expression));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> list;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="六、JWT"><a href="#六、JWT" class="headerlink" title="六、JWT"></a>六、JWT</h2><h3 id="6-1-JWT-介绍"><a href="#6-1-JWT-介绍" class="headerlink" title="6.1 JWT 介绍"></a>6.1 JWT 介绍</h3><p>jsonwebtoken（JWT）是一个开放标准（rfc7519），它定义了一种紧凑的、自包含的方式，用于在各方之间以 JSON 对象安全地传输信息。此信息可以验证和信任，因为它是数字签名的。jwt 可以使用秘密（使用 HMAC 算法）或使用 RSA 或 ECDSA 的公钥&#x2F;私钥对进行签名</p>
<p>通俗的讲： JWT 简称 JSON Web Token,也就是通过 JSON 形式作为 Web 应用中的令牌,用于在各方之间安全地将信息作为 JSON 对象传输。在数据传输过程中还可以完成数据加密、签名等相关处理。</p>
<h3 id="6-4-JWT-使用"><a href="#6-4-JWT-使用" class="headerlink" title="6.4 JWT 使用"></a>6.4 JWT 使用</h3><h4 id="6-4-1-引入依赖"><a href="#6-4-1-引入依赖" class="headerlink" title="6.4.1 引入依赖"></a>6.4.1 引入依赖</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--引入jwt--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.auth0<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>java-jwt<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.4.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="七、附录：HttpSecurity-配置项"><a href="#七、附录：HttpSecurity-配置项" class="headerlink" title="七、附录：HttpSecurity 配置项"></a>七、附录：HttpSecurity 配置项</h2><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>openidLogin()</strong></td>
<td>用于基于 OpenId 的验证</td>
</tr>
<tr>
<td><strong>headers()</strong></td>
<td>将安全标头添加到响应</td>
</tr>
<tr>
<td><strong>cors()</strong></td>
<td>配置跨域资源共享（ CORS ）</td>
</tr>
<tr>
<td><strong>sessionManagement()</strong></td>
<td>允许配置会话管理</td>
</tr>
<tr>
<td><strong>portMapper()</strong></td>
<td>向到 HTTPS 或者从 HTTPS 重定向到 HTTP。默认情况下，Spring Security 使用一个 PortMapperImpl 映射 HTTP 端口 8080 到 HTTPS 端口 8443，HTTP 端口 80 到 HTTPS 端口 443</td>
</tr>
<tr>
<td><strong>jee()</strong></td>
<td>配置基于容器的预认证。 在这种情况下，认证由 Servlet 容器管理</td>
</tr>
<tr>
<td><strong>x509()</strong></td>
<td>配置基于 x509 的认证</td>
</tr>
<tr>
<td><strong>rememberMe</strong></td>
<td>允许配置“记住我”的验证</td>
</tr>
<tr>
<td><strong>authorizeRequests()</strong></td>
<td>允许基于使用 HttpServletRequest 限制访问</td>
</tr>
<tr>
<td><strong>requestCache()</strong></td>
<td>允许配置请求缓存</td>
</tr>
<tr>
<td><strong>exceptionHandling()</strong></td>
<td>允许配置错误处理</td>
</tr>
<tr>
<td><strong>securityContext()</strong></td>
<td>在 HttpServletRequests 之间的 SecurityContextHolder 上设置 SecurityContext 的管理。 当使用 WebSecurityConfifigurerAdapter 时，这将</td>
</tr>
<tr>
<td><strong>servletApi()</strong></td>
<td>将 HttpServletRequest 方法与在其上找到的值集成到 SecurityContext 中。 当使用 WebSecurityConfifigurerAdapter 时，这将自动应用</td>
</tr>
<tr>
<td><strong>csrf()</strong></td>
<td>添加 CSRF 支持，使用 WebSecurityConfifigurerAdapter 时，默认启用</td>
</tr>
<tr>
<td><strong>logout()</strong></td>
<td>添加退出登录支持。当使用 WebSecurityConfifigurerAdapter 时，这将自动应用。默认情况是，访问 URL”&#x2F; logout”，使 HTTP Session 无效来</td>
</tr>
<tr>
<td><strong>anonymous()</strong></td>
<td>允许配置匿名用户的表示方法。 当与 WebSecurityConfifigurerAdapter 结合使用时，这将自动应用。 默认情况下，匿名用户将使用</td>
</tr>
<tr>
<td><strong>formLogin()</strong></td>
<td>指定支持基于表单的身份验证。如果未指定 FormLoginConfifigurer#loginPage(String)，则将生成默认登录页面</td>
</tr>
<tr>
<td><strong>oauth2Login()</strong></td>
<td>根据外部 OAuth 2.0 或 OpenID Connect 1.0 提供程序配置身份验证</td>
</tr>
<tr>
<td><strong>requiresChannel()</strong></td>
<td>配置通道安全。为了使该配置有用，必须提供至少一个到所需信道的映射</td>
</tr>
<tr>
<td><strong>httpBasic()</strong></td>
<td>配置 Http Basic 验证</td>
</tr>
<tr>
<td><strong>addFilterAt()</strong></td>
<td>允许配置错误处理</td>
</tr>
<tr>
<td><strong>exceptionHandling()</strong></td>
<td>在指定的 Filter 类的位置添加过滤器</td>
</tr>
</tbody></table>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
</search>
